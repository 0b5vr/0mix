import { BufferTextureRenderTarget } from '../heck/BufferTextureRenderTarget';

export const cameraStackATarget = new BufferTextureRenderTarget( 4, 4 );
export const cameraStackBTarget = new BufferTextureRenderTarget( 4, 4 );
